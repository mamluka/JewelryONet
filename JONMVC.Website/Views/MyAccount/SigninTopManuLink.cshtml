@{
    Layout = null;
}
@if (ViewBag.IsSignedIn)
{
    @Html.ActionLink("My Account", "Index", "MyAccount") 
    @:&#160;
    @Html.ActionLink("(Sign Out)", "Signout", "MyAccount")
    
}
else
{
    @Html.ActionLink("Sign In", "Signin", "MyAccount") 
}
